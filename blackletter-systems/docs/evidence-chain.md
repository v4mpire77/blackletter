# Evidence Chain

Each flag must include: `(rule_id, source_url, source_version, approver_id, timestamp)`.
- Append-only changelog with cryptographic hash.
- Exportable evidence bundle (JSON/PDF).
- Dual control for high-severity updates.
