name: CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    env:
copilot/fix-661187ed-e39a-4291-a25f-91e90f68491d
      CARGO_HOME: $HOME/.cargo
      RUSTUP_HOME: $HOME/.rustup
    

      CARGO_HOME: ${{ env.HOME }}/.cargo
      RUSTUP_HOME: ${{ env.HOME }}/.rustup
main
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Rust
        run: |
copilot/fix-661187ed-e39a-4291-a25f-91e90f68491d

codex/fix-rust-toolchain-writable-paths
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          source "$CARGO_HOME/env"
          rustc --version

      # Other steps can be added here

          echo "CARGO_HOME=$HOME/.cargo" >> $GITHUB_ENV
          echo "RUSTUP_HOME=$HOME/.rustup" >> $GITHUB_ENV
main
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          rustc --version

copilot/fix-661187ed-e39a-4291-a25f-91e90f68491d
      - name: Upgrade Pip
        run: pip install --upgrade pip

      - name: Install Dependencies
        run: |
          if [ -f backend/requirements.txt ]; then
            pip install -r backend/requirements.txt
          fi
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi


      # Other steps can be added here
      - name: Upgrade pip
        run: pip install --upgrade pip

      - name: Install dependencies
        run: |
          pip install maturin
          pip install -r backend/requirements.txtmain
main
