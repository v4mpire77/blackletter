services:
  - type: web
    name: blackletter
    region: oregon
    plan: free
    branch: main
    runtime: python
    buildCommand: |
      pip install --upgrade pip
      pip install -r src/backend/requirements.txt
    startCommand: |
      export PYTHONPATH=$PWD
      cd src
      gunicorn -k uvicorn.workers.UvicornWorker backend.main:app --bind 0.0.0.0:$PORT --workers 4
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: BACKEND_URL
        value: https://blackletter-backend.onrender.com/review
